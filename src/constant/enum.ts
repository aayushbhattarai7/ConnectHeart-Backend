export enum Environment {
  DEVELOPMENT = 'DEVELOPMENT',
  PRODUCTION = 'PRODUCTION',
}

export enum MediaType {
  PROFILE = 'PROFILE',
  POST = 'POST',
  CHAT = 'CHAT',
}

export enum Role {
  ADMIN = 'ADMIN',
  USER = 'USER',
}
export enum Status {
  ACCEPTED = 'ACCEPTED',
  PENDING = 'PENDING',
}

export enum Gender {
  MALE = 'MALE',
  FEMALE = 'FEMALE',
}
